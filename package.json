{
  "name": "fnol",
  "private": true,
  "scripts": {
    "dev": "turbo dev --parallel & (cd backend && uv run uvicorn main:app --reload --port 8000)",
    "dev:frontend": "turbo dev",
    "dev:backend": "cd backend && uv run uvicorn main:app --reload --port 8000",
    "build": "turbo build",
    "lint": "turbo lint",
    "setup": "pnpm install && cd backend && uv pip install -r requirements.txt"
  },
  "devDependencies": {
    "turbo": "^2.3.3"
  },
  "packageManager": "pnpm@9.15.0"
}
